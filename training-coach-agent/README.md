# training-coach-agent

TypeScript HTTP service for a running coach agent built on top of `coros-fit-mcp` and an OpenAI-compatible model API.

## Scripts

- `npm run dev`
- `npm run build`
- `npm run check`

## Environment

Copy `.env.example` to `.env` and adjust values before starting the service.
